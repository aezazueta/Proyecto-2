# Segmentacion_clientes

Proyecto de segmentación de clientes a través del entrenamiento del modelo de clusterización K-means. 
Se carga el dataset, se limpia, prepara y organiza mediante el modelo de segmentacion RFM y posteriormente usarlo para entrenar el modelo K-means.
